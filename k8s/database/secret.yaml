apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: default
type: Opaque
stringData:
  # In a real production environment, use a sealed secret or external secret manager
  POSTGRES_USER: gitops_user
  POSTGRES_PASSWORD: gitops_password
  POSTGRES_DB: gitopsdb

